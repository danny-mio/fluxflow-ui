[pytest]
# FluxFlow UI - Test Configuration

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    --verbose
    --tb=short
    --strict-markers
    --color=yes
    -ra

markers =
    slow: marks tests as slow (>1s execution time)

timeout = 300
timeout_method = thread
